document.addEventListener("DOMContentLoaded", () => {

    const loginButton =
        document.getElementById("loginButton");

    loginButton.addEventListener("click", () => {

        alert(
            "Google Workspace authentication will be connected in the next step."
        );

    });

});
